
const errorsReducer = (state = {}, action) => {
    switch (action.type) {
        case "banana":
            return state;
        default:
            return state;
    }
}

export default errorsReducer;